Macとスマホは別々のStreamlitセッションですが、shared_trip_state.jsonを共通で読み書きします。
どちらかで予定を変更・削除・日程変更すると、共有ファイルへ即時保存されます。
もう片方では画面を更新するか「最新データを取得」を押すと反映されます。
同時に両方で編集すると、最後に保存した側の内容が優先されます。
URLは起動するたびに変わります。Macとターミナルは起動したままにしてください。
